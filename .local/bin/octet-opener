#!/bin/bash

print_model() {
	temp_file="/tmp/model.temp"
	model_info "$1" >"$temp_file" && $EDITOR "$temp_file"
}

case "$1" in
*.mp4) setsid -f mpv "$1" -quiet >/dev/null 2>&1 ;;
*.encrypted) print-model "$1" ;;
esac
